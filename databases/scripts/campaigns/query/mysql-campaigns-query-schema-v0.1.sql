-- MySQL Script generated by MySQL Workbench
-- Sat Oct 07 00:00:00 2020
-- Model: New Model    Version: 1.0
-- MySQL Workbench Forward Engineering

SET @OLD_UNIQUE_CHECKS=@@UNIQUE_CHECKS, UNIQUE_CHECKS=0;
SET @OLD_FOREIGN_KEY_CHECKS=@@FOREIGN_KEY_CHECKS, FOREIGN_KEY_CHECKS=0;
SET @OLD_SQL_MODE=@@SQL_MODE, SQL_MODE='TRADITIONAL,ALLOW_INVALID_DATES';

-- -----------------------------------------------------
-- Schema campaignsquerydb
-- -----------------------------------------------------
-- Base de datos que permite gestionar la informacion de productos
DROP SCHEMA IF EXISTS `campaignsquerydb` ;

-- -----------------------------------------------------
-- Schema Products Command 
--
-- Base de datos que permite gestionar la informacion de productos
-- -----------------------------------------------------
CREATE SCHEMA IF NOT EXISTS `campaignsquerydb` DEFAULT CHARACTER SET utf8 ;
SHOW WARNINGS;

USE `campaignsquerydb` ;

CREATE TABLE IF NOT EXISTS CAMPAIGNS (
    CAMPAIGNS_ID   VARCHAR(512) NOT NULL,
	CAMPAIGNS_CODE VARCHAR(512) NOT NULL,
	CAMPAIGNS_NAME VARCHAR(512) NOT NULL,
	CAMPAIGNS_DESC VARCHAR(1500) NOT NULL,
	IMAGE_ID       VARCHAR(512) NOT NULL,
	START_DATE	   DATE NOT NULL,
	END_DATE       DATE NOT NULL,
    DISCOUNT	   BIGINT NOT NULL,
	STATUS 		   VARCHAR(100)	
) ENGINE = InnoDB;

CREATE TABLE IF NOT EXISTS CAMPAIGNS_PRODUCTS (
	CAMPAIGNS_PRODUCTS_ID VARCHAR(512) NOT NULL,
	CAMPAIGNS_ID VARCHAR(512) NOT NULL,
	PRODUCT_ID	 VARCHAR(512) NOT NULL
) ENGINE = InnoDB;

ALTER TABLE CAMPAIGNS ADD CONSTRAINT PK_CPG PRIMARY KEY (CAMPAIGNS_ID);
ALTER TABLE CAMPAIGNS ADD INDEX(CAMPAIGNS_CODE);

ALTER TABLE CAMPAIGNS ADD FULLTEXT (CAMPAIGNS_CODE, CAMPAIGNS_NAME, CAMPAIGNS_DESC);

ALTER TABLE CAMPAIGNS_PRODUCTS ADD CONSTRAINT PK_PGP PRIMARY KEY (CAMPAIGNS_PRODUCTS_ID);

ALTER TABLE CAMPAIGNS_PRODUCTS ADD CONSTRAINT FK_CPG_CGP FOREIGN KEY (CAMPAIGNS_ID) REFERENCES CAMPAIGNS (CAMPAIGNS_ID);